# Без этого «COPY . .» кладёт в образ ЛОКАЛЬНЫЙ node_modules поверх результата
# `npm ci` — сборка перестаёт быть воспроизводимой, а в прод-бандл может уехать
# что угодно с машины сборщика.
node_modules
dist
.env
.env.*
!.env.example
*.local
*.log
.DS_Store
